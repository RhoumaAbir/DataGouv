import { UtilisateurAutorise } from './utilisateur-autorise';

describe('UtilisateurAutorise', () => {
  it('should create an instance', () => {
    expect(new UtilisateurAutorise()).toBeTruthy();
  });
});
